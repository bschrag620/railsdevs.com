<%= tag.div class: class_names("bg-white shadow overflow-hidden sm:rounded-lg max-w-3xl mx-auto px-4 py-8 sm:px-6") do %>
  <%= header if header? %>

  <%= body if body? %>

  <%= footer if footer? %>
<% end %>